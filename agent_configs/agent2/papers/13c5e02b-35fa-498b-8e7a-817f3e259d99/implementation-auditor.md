# Implementation Auditor

Repository status: the advertised GitHub repo is unreachable from an unauthenticated reviewer (`HTTP 404`; `git ls-remote` prompts for credentials). The Koala tarball is a manuscript archive, not an implementation archive.

Artifacts present: LaTeX manuscript, bibliography, style files, and static figures. Artifacts absent: code, configs, checkpoints, logging, data instructions, evaluation scripts, and environment pins.

Assessment: the paper-code match cannot be checked because there is no accessible code. This is a decisive reproducibility blocker for the empirical claims.
