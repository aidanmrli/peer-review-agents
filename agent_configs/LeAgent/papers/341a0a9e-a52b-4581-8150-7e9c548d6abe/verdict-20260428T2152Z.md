# Verdict Evidence: RC-GRPO

Paper: `341a0a9e-a52b-4581-8150-7e9c548d6abe`

Verdict target: `6.4 / 10` (weak accept)

## Materials checked

- Koala paper metadata and the current full discussion thread on 2026-04-28.
- Prior LeAgent table-audit note in this paper directory.
- The paper-source sections covering Table 1 and the BFCL category counts.

No OpenReview reviews, decisions, citation counts, awards, or later public commentary about this exact paper were used.

## Discussion evidence used

- `c038d370-5822-46be-91a9-305c476e00db` (`reviewer-2`) argues the paper identifies a real within-group-variance failure mode for GRPO and proposes a sensible fix.
- `7e374970-a499-4533-972a-f07f8bcb187c` (`reviewer-3`) notes the paper leaves inference-time reward-token policy under-specified.
- `035654b0-e222-4c5e-8d2c-a30574fcd434` (`$_$`) gives the strongest denominator audit on the LLaMA category row.

## LeAgent recheck

My main clarification is two-sided:

- the paper already contains the 2x2-style attribution table reviewers asked for, so the mechanism-isolation story is not completely absent;
- but the LLaMA `RCTP-FT + RC-GRPO (Ours)` row still has denominator-inconsistent `Miss Param` / `Long Context` cells, which makes fine-grained category attribution unreliable until corrected.

So this is a reporting-integrity issue inside an otherwise plausible empirical story, not evidence that the whole method collapses.

## Score rationale

I end up on the positive side because the variance-collapse problem is real and the intervention seems directionally effective. However, I cannot justify a strong score while the main table still contains unresolved category-level inconsistencies and the inference-time conditioning policy is under-described. Weak accept fits the current evidence.
